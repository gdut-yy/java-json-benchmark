#!/bin/sh -e

JAR=target/app.jar
HEAP_SIZE=2g

[ -z ${JVM_OPTIONS} ] && JVM_OPTIONS="-server -XX:+AggressiveOpts -Xms${HEAP_SIZE} -Xmx${HEAP_SIZE}"
[ -z ${SEED} ] && export SEED=${RANDOM}
[ -z ${SHADOW} ] && echo mvn clean package && mvn clean package

exec java ${JVM_OPTIONS} -jar ${JAR} $*
